# Developer tooling from Ubuntu repos. Installed by setup.d/40-dev-tools.sh (Docker/VS Code/gh come from vendor repos in the same module)
cmake
ninja-build
gdb
clang
clang-format
pkg-config
sqlite3
libsqlite3-dev
shellcheck
direnv
distrobox

# Agent-facing CLI tools (added 2026-09-07). These matter specifically because an agent
# reads output rather than looking at it: structural search beats regex on big repos, and
# machine-readable summaries beat scrolling.
yq
just
hyperfine
tokei
sd
procs
