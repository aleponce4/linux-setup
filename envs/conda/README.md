# conda environment specs

Exported 2026-09-02 from the WSL Ubuntu 24.04 mambaforge install (conda env export --from-history).
Recreate with pixi (pixi init --import <file>) or micromamba/mamba (mamba env create -f <file>).
Project-specific envs (libs-ploidy, jonsson-bioinfo) live inside their git repos and are not duplicated here.
